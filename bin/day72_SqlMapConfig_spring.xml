<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	
	<!-- 如果mapper中不使用别名，这个文件可以省略 -->
	<typeAliases>
		<package name="day72_mybatis.demo2_mybatisspring.entity"/>
		<package name="day72_mybatis.demo.eneity"/>
	</typeAliases>

	<!-- mappers（映射器） -->
	<!-- 加载mapper.xml -->
	<!-- <mappers>
		<mapper resource="day72sqlmap/user.xml"/>
		<package name="day72_mybatis.demo2_mybatisspring.mapper"/>
		<package name="day72_mybatis.demo.mapper"/>
	</mappers> -->

</configuration>